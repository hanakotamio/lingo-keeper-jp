# ===== Backend Environment Variables Template =====
# Copy this file to .env.local and fill in your values
# WARNING: Never commit .env.local to version control

# ===== Database =====
# Neon PostgreSQL connection string
# Format: postgresql://[user]:[password]@[host]/[database]?sslmode=require
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# ===== Google Cloud Platform =====
# GCP Project ID
GOOGLE_CLOUD_PROJECT_ID=your-gcp-project-id

# Path to GCP Service Account Key (JSON file)
# For local development: absolute path
# For Cloud Run: use Secret Manager instead
GOOGLE_APPLICATION_CREDENTIALS=/path/to/gcp-service-account-key.json

# ===== OpenAI API =====
# OpenAI API Key for quiz auto-generation
OPENAI_API_KEY=sk-proj-...

# ===== Authentication (Phase 2+) =====
# JWT secret for token signing (generate with: openssl rand -base64 32)
JWT_SECRET=your-jwt-secret-here

# Session secret for session management (generate with: openssl rand -base64 32)
SESSION_SECRET=your-session-secret-here

# ===== Application Settings =====
# Environment: development | production | test
NODE_ENV=development

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3847

# Backend URL (optional, for health checks)
BACKEND_URL=http://localhost:8534

# ===== CORS Settings =====
# Allowed origin for CORS (IMPORTANT: Do not hardcode in production!)
CORS_ORIGIN=http://localhost:3847

# ===== Cloud Run Specific (Production) =====
# Port (Cloud Run sets this automatically, default: 8080)
# PORT=8080
