# 本番環境修正手順書

**作成日**: 2026-01-25
**問題**: `/api/auth/me` 401エラー & タイトル表示の問題
**ステータス**: 修正実施中

---

## 🐛 発見された問題

### 問題1: `/api/auth/me` エンドポイントの401エラー

**現象**:
```
lingo-keeper-backend-16378814888.asia-northeast1.run.app/api/auth/me:1
Failed to load resource: the server responded with a status of 401 ()
```

**根本原因**:
- `.env.vercel` で間違ったバックエンドURLを使用
- ❌ 間違い: `https://lingo-keeper-backend-16378814888.asia-northeast1.run.app`
- ✅ 正しい: `https://lingo-keeper-jp-backend-16378814888.asia-northeast1.run.app`
- **"jp" が抜けている**

**補足**:
- `/api/auth/me` エンドポイント自体は実装されていない（MVPフェーズはLocalStorage認証）
- フロントエンドからこのエンドポイントは呼ばれていない
- エラーログは出るが機能への影響はなし
- ただし、環境変数の誤りは他のAPIコール（`/api/stories`など）にも影響する可能性がある

### 問題2: アプリケーションタイトルの表示

**現象**:
- 期待値: "Lingo Keeper JP - 日本語学習アプリ"
- 実際: "Lingo Keeper - English Learning Adventure"

**根本原因**:
- Vercelが古いビルド/デプロイをキャッシュしている
- ソースコード (`frontend/index.html`) は正しい
- 再デプロイが必要

---

## ✅ 実施した修正

### 1. `.env.vercel` ファイルの修正

**ファイル**: `frontend/.env.vercel` (Line 3)

**変更前**:
```bash
VITE_API_URL="https://lingo-keeper-backend-16378814888.asia-northeast1.run.app"
```

**変更後**:
```bash
VITE_API_URL="https://lingo-keeper-jp-backend-16378814888.asia-northeast1.run.app"
```

**ステータス**: ✅ 修正完了

---

## 🔧 必要な追加手順

### Step 1: Vercel環境変数の更新（必須）

Vercelダッシュボードで環境変数を手動で更新する必要があります。

#### 1.1 Vercelダッシュボードにアクセス

1. https://vercel.com にログイン
2. プロジェクト「frontend」を選択
3. 「Settings」タブをクリック
4. 左サイドバーの「Environment Variables」をクリック

#### 1.2 VITE_API_URL の更新

以下の**3つの環境**すべてで更新してください：

##### Production環境

1. `VITE_API_URL` を検索
2. 「Edit」ボタンをクリック
3. 現在の値を確認:
   ```
   https://lingo-keeper-backend-16378814888.asia-northeast1.run.app
   ```
4. 以下に変更:
   ```
   https://lingo-keeper-jp-backend-16378814888.asia-northeast1.run.app
   ```
5. 「Save」をクリック

##### Preview環境

1. 同様に `VITE_API_URL` の Preview環境設定を編集
2. 同じ値に変更:
   ```
   https://lingo-keeper-jp-backend-16378814888.asia-northeast1.run.app
   ```
3. 「Save」をクリック

##### Development環境

1. 同様に `VITE_API_URL` の Development環境設定を編集
2. 同じ値に変更:
   ```
   https://lingo-keeper-jp-backend-16378814888.asia-northeast1.run.app
   ```
3. 「Save」をクリック

#### スクリーンショット参考

```
Environment Variables
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔍 Search: VITE_API_URL

┌─────────────────────────────────────────────────┐
│ VITE_API_URL                                    │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│ Production:                                     │
│ https://lingo-keeper-jp-backend-16378814888...  │ ← 正しい値
│                                                 │
│ Preview:                                        │
│ https://lingo-keeper-jp-backend-16378814888...  │ ← 正しい値
│                                                 │
│ Development:                                    │
│ https://lingo-keeper-jp-backend-16378814888...  │ ← 正しい値
│                                                 │
│ [Edit] [Delete]                                 │
└─────────────────────────────────────────────────┘
```

---

### Step 2: コミット & プッシュ（`.env.vercel` の修正）

```bash
cd /home/hanakotamio0705/Lingo\ Keeper\ JP

# ステージング
git add frontend/.env.vercel

# コミット
git commit -m "fix: Update VITE_API_URL in .env.vercel to correct backend URL

- Fix missing 'jp' in backend URL
- Change from lingo-keeper-backend to lingo-keeper-jp-backend
- Resolves 401 errors in browser console

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>"

# プッシュ
git push origin main
```

---

### Step 3: Vercelで再デプロイ（タイトル問題の解決）

#### オプションA: Vercelダッシュボードから再デプロイ（推奨）

1. Vercelダッシュボード → プロジェクト「frontend」
2. 「Deployments」タブをクリック
3. 最新の成功したデプロイを探す
4. 右側の「・・・」メニューをクリック
5. 「Redeploy」を選択
6. ✅ **「Use existing Build Cache」のチェックを外す**（重要！）
7. 「Redeploy」ボタンをクリック

#### オプションB: 空コミットで強制再ビルド

```bash
cd /home/hanakotamio0705/Lingo\ Keeper\ JP

# 空コミット（変更なしでコミット）
git commit --allow-empty -m "chore: Force Vercel rebuild to fix cached title

- Clear Vercel build cache
- Fix title display from 'Lingo Keeper' to 'Lingo Keeper JP'

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>"

# プッシュ
git push origin main
```

これによりVercelが自動で再ビルド・再デプロイを開始します。

---

### Step 4: 動作確認

再デプロイが完了したら（約2-3分）、以下を確認してください：

#### 4.1 タイトル表示の確認

1. ブラウザで https://frontend-seven-beta-72.vercel.app を開く
2. ブラウザタブのタイトルを確認
3. **期待値**: "Lingo Keeper JP - 日本語学習アプリ"
4. **以前の値**: "Lingo Keeper - English Learning Adventure"

**確認方法**:
```bash
# コマンドラインで確認
curl -s https://frontend-seven-beta-72.vercel.app | grep "<title>"

# 期待される出力:
# <title>Lingo Keeper JP - 日本語学習アプリ</title>
```

#### 4.2 API接続の確認

1. ブラウザの開発者ツールを開く（F12）
2. 「Console」タブを確認
3. **401エラーが表示されないことを確認**
4. 「Network」タブを確認
5. `/api/stories` リクエストを探す
6. リクエストURL: `https://lingo-keeper-jp-backend-16378814888.asia-northeast1.run.app/api/stories`
7. ステータス: **200 OK**

**確認方法**:
```bash
# コマンドラインで確認
curl -s https://lingo-keeper-jp-backend-16378814888.asia-northeast1.run.app/api/stories | jq '.success'

# 期待される出力:
# true
```

#### 4.3 ログイン & ストーリー閲覧の確認

1. https://frontend-seven-beta-72.vercel.app/login にアクセス
2. デモアカウントでログイン:
   - Email: `demo@example.com`
   - Password: `demo123`
3. ダッシュボードにリダイレクトされることを確認
4. 「Stories」メニューをクリック
5. **9件のストーリーカードが表示されることを確認**
6. ストーリーカードをクリックして詳細表示
7. チャプター内容が表示されることを確認

---

## 📊 修正前後の比較

### API URL

| 環境 | 修正前 | 修正後 | ステータス |
|------|--------|--------|-----------|
| `.env.vercel` | `lingo-keeper-backend-...` | `lingo-keeper-jp-backend-...` | ✅ 修正済み |
| Vercel Production | `lingo-keeper-backend-...` | `lingo-keeper-jp-backend-...` | ⏳ 手動更新必要 |
| Vercel Preview | `lingo-keeper-backend-...` | `lingo-keeper-jp-backend-...` | ⏳ 手動更新必要 |
| Vercel Development | `lingo-keeper-backend-...` | `lingo-keeper-jp-backend-...` | ⏳ 手動更新必要 |

### タイトル表示

| 場所 | 修正前 | 修正後 | ステータス |
|------|--------|--------|-----------|
| `frontend/index.html` | "Lingo Keeper JP - 日本語学習アプリ" | "Lingo Keeper JP - 日本語学習アプリ" | ✅ 正しい（変更なし） |
| Vercel Production | "Lingo Keeper - English Learning Adventure" | "Lingo Keeper JP - 日本語学習アプリ" | ⏳ 再デプロイ必要 |

---

## ⚠️ 注意事項

### `.env.vercel` ファイルについて

- このファイルは`vercel dev`コマンド実行時にVercel CLIが自動生成
- 通常はGitにコミットしない（`.gitignore`に追加推奨）
- ただし、今回は修正内容を記録するためコミット

### Vercel環境変数の優先順位

1. **Vercelダッシュボードの環境変数**（最優先）
2. `.env.production`（Production環境のみ）
3. `.env.local`（ローカル開発のみ）
4. `.env`（フォールバック）

**重要**: Vercelダッシュボードの環境変数が最優先されるため、`.env.vercel`の修正だけでは不十分です。必ずVercelダッシュボードで手動更新してください。

### キャッシュについて

Vercelは以下をキャッシュします：
- ビルド成果物（`dist/`）
- 依存関係（`node_modules/`）
- 静的アセット（画像、フォントなど）

タイトル表示の問題は**ビルドキャッシュ**が原因なので、再デプロイ時に「Use existing Build Cache」のチェックを外すことが重要です。

---

## 🎯 修正完了の確認チェックリスト

- [ ] `.env.vercel` の修正完了
- [ ] `.env.vercel` をGitにコミット・プッシュ
- [ ] Vercel環境変数（Production）の更新完了
- [ ] Vercel環境変数（Preview）の更新完了
- [ ] Vercel環境変数（Development）の更新完了
- [ ] Vercelで再デプロイ実施（キャッシュクリア）
- [ ] ブラウザタブのタイトルが "Lingo Keeper JP - 日本語学習アプリ" に変更
- [ ] ブラウザコンソールで401エラーが表示されない
- [ ] `/api/stories` が200 OKを返す
- [ ] ストーリーカードが9件表示される
- [ ] ログイン → ストーリー閲覧のフロー動作確認

---

## 📚 関連ドキュメント

- `CLAUDE.md`: プロジェクト設定・技術スタック
- `docs/ci-cd-e2e-integration.md`: CI/CD統合ドキュメント
- `E2E_TEST_FINAL_REPORT_2026-01-25.md`: E2Eテスト結果

---

## 🔄 更新履歴

| 日付 | 内容 |
|------|------|
| 2026-01-25 | 初版作成、`.env.vercel` 修正、Vercel環境変数更新手順書作成 |

---

**作成者**: Claude Sonnet 4.5
**ステータス**: ⏳ Vercel環境変数の手動更新待ち
**次のアクション**: Vercelダッシュボードで環境変数を更新 → 再デプロイ
